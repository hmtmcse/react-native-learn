

```
npx create-expo-app bismillah
cd bismillah
npm run android
npm run ios
npm run web


```



```
npx create-expo-app@latest --template default@next

pnpm dlx create-expo-app@latest --template default@next
```

== emulator
```
emulator -list-avds
emulator -avd Pixel_6_Pro
adb devices
adb -s emulator-5554 emu kill

emulator -avd Pixel_5_API_33 -gpu auto -no-snapshot-load
```

== Expo
```
npm install -g expo-cli
```

== Android Build
```
npx expo prebuild
cd android
gradlew assembleRelease
adb install -r app/build/outputs/apk/release/app-release.apk

# Dev Environement
pnpm expo run:android
```

== pnpm
```
pnpm create expo-app myApp
cd myApp
pnpm install
pnpm start
```

== Drawer
```
pnpm create expo-app rnn-drawer --template expo-template-blank-typescript
cd rnn-drawer
pnpm install
pnpm add @react-navigation/drawer @react-navigation/native @react-navigation/native-stack react-native-gesture-handler react-native-reanimated react-native-screens react-native-safe-area-context react-native-vector-icons
```